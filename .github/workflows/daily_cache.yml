name: MLB Cache Refresh

on:
  # Run daily at 9:00 AM ET (14:00 UTC)
  schedule:
    - cron: '0 14 * * *'

  # Allow manual trigger from GitHub UI anytime
  workflow_dispatch:

jobs:
  refresh-cache:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pandas pybaseball requests pyarrow

      - name: Run cache fetcher
        env:
          MLB_CACHE_DIR: /tmp/mlb_cache
        run: python scripts/fetch_cache.py

      - name: Copy cache to repo cache/ folder
        run: |
          mkdir -p cache/daily cache/weekly
          cp -r /tmp/mlb_cache/daily/*.parquet  cache/daily/  2>/dev/null || true
          cp -r /tmp/mlb_cache/weekly/*.parquet cache/weekly/ 2>/dev/null || true
          echo "Files in cache/:"
          ls -lh cache/daily/ cache/weekly/ || true

      - name: Commit and push cache files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cache/
          git diff --staged --quiet || git commit -m "cache: refresh $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push
